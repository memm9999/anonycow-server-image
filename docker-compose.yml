services:
  anonycow-server:
    build: .
    image: "memm9999/anonycow-server:aca"
    command:
      - /bin/sh
      - -c
      - |
        npx prisma db push
        create-admin --username=mohammed --password=123456789
        nohup ./runPrismaStudio.sh &
        node index.js
    ports:
      - "80:80"
